import React from 'react';
import styled from 'styled-components';

const StyledWrapper = styled.div`
  padding: 17px 30px;
`;

const Card = () => (
  <StyledWrapper>
    Lorem ipsum
  </StyledWrapper>
);

export default Card;
